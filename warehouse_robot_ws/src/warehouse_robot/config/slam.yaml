slam_toolbox:
  ros__parameters:
    use_sim_time: true

    mode: mapping

    map_frame: map
    odom_frame: odom
    base_frame: base_footprint

    scan_topic: /scan

    resolution: 0.05
    max_laser_range: 10.0
    minimum_time_interval: 0.5
    transform_publish_period: 0.02
